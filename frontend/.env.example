# Base URL of the id backend (OIDC provider API)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional: custom GIF played fullscreen during a light/dark theme swap.
# Leave empty to use the View-Transition circle reveal instead.
NEXT_PUBLIC_THEME_GIF_URL=
